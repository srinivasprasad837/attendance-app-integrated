{
  "name": "attendanceapp-server",
  "version": "1.0.0",
  "description": "attendance app server",
  "main": "server.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "dev": "nodemon server.js",
    "dev-build": "cd Attendance-app-UI && npm install && npm run build && cp -r dist/* ../public/ && cd .. && npm install",
    "start": "node server.js"
  },
  "author": "srnivas prasad",
  "license": "ISC",
  "dependencies": {
    "@emotion/react": "^11.14.0",
    "@emotion/styled": "^11.14.0",
    "@mui/material": "^6.4.4",
    "cors": "^2.8.5",
    "dotenv": "^16.4.7",
    "express": "^4.21.2",
    "mongoose": "^8.10.1",
    "nodemon": "^3.1.9"
  }
}
