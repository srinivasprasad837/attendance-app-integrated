{
  "name": "attendanceapp-server",
  "version": "1.0.0",
  "description": "attendance app server",
  "main": "server.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "dev": "nodemon server/server.js",
    "start": "cd Attendance-app-UI && npm run build && cp -r dist/* ../public/ && cd .. && node server.js"
  },
  "author": "mannu",
  "license": "ISC",
  "dependencies": {
    "cors": "^2.8.5",
    "dotenv": "^16.4.7",
    "express": "^4.21.2",
    "nodemon": "^3.1.9"
  }
}
