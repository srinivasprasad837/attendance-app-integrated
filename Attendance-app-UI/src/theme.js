import { createTheme } from '@mui/material/styles';

const theme = createTheme({
  palette: {
    primary: {
      main: '#9933ff', // Default Material UI blue
    },
    secondary: {
      main: '#ffffff', // White
    },
  },
});

export default theme;
